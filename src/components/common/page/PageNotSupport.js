import React from 'react';

import styles from './PageNotSupport.module.scss';

const PageNotSupport = () => {
    return (
        <div className={styles.pageNotSupport}>
            <div>-1</div>
            <div>Not support yet</div>
        </div>
    );
};

export default PageNotSupport;
